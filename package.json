{
  "name": "bookmark-nav",
  "version": "1.0.0",
  "description": "书签导航 - 支持 Docker 和 Vercel 部署",
  "scripts": {
    "dev": "cd backend && node server.js",
    "build": "echo 'No build step required'",
    "db:init": "node scripts/init-mysql.js",
    "db:migrate": "node scripts/migrate-to-mysql.js"
  },
  "dependencies": {
    "better-sqlite3": "^11.7.0",
    "cheerio": "^1.1.2",
    "compression": "^1.7.4",
    "cors": "^2.8.5",
    "express": "^4.21.2",
    "mysql2": "^3.11.0",
    "node-fetch": "^2.7.0"
  },
  "engines": {
    "node": ">=18"
  }
}
